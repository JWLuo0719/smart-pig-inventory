# 冻结的 Django 原型

该目录是路线 A 阶段形成的验证性原型，仅保留用于回归上传合同、媒体锁定、ROI、审计和 Provider 思想。

- 新业务功能统一进入 `services/business-api/`。
- 不再为该目录增加生产能力或数据库迁移。
- 迁移某项行为前，先把行为写成合同测试，再在 Spring Boot 中独立实现。
- 禁止把此原型与 Spring 主服务同时连接到同一业务库。

待 Spring 上传闭环和验收用例完全覆盖后，再由项目负责人决定是否归档该目录；当前不要删除。
