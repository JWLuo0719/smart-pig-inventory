# 原始盘点需求功能收口（2026-09-12）

## 2026-09-13 非视频功能验收推进

用户反馈前四项非视频功能测试成功，视频相关功能后置。上传恢复、人工复核、图库/同步/报表等非视频边界继续按已验证证据推进；视频证据流程和视频自动计数仍不计入本轮通过，模型批准状态保持关闭。

## 2026-09-13 v18 真机上传恢复闭环

步骤 2 卡住的本地上传包在 v18 中完成真机恢复：启动时仅对确有可同步任务的队列重新安排 WorkManager，网络/握手/超时仍按 `retry_wait` 处理并清理租约。原包未点重试或放弃，服务端 Commit 成功，会话进入人工复核，候选 21；移动端重启读回已提交且待上传为 0。上传可靠性增量已完成，人工清点/确认和正式目标部署仍按原边界执行。

## 2026-09-13 v17 上传包重试可靠性增量

真机步骤 2 暴露本地上传包在网络/握手异常后保持 `creating_package` 的恢复缺口。移动端现将这些暂时性异常统一写入 `retry_wait`，清除租约并保留原图、幂等键和已完成分片；重试时间更新使用类型化 Drift 参数。该修复只改善本地可靠性，不改变服务端候选、人工确认、锁定或模型批准边界。v17 Release 候选已构建，设备覆盖安装和原包自动恢复读回待无线 ADB 恢复。

## 2026-09-13 真机反馈修复与 v16 候选

v16 已在 Redmi K60 上完成同签名覆盖安装和冷启动读回。认证刷新后首页能自动拉取当日真实任务，服务端 `/me` 与 `inventory-tasks` 均返回 200，UI 显示两个任务和 `0 / 2`。真机反馈暴露的生命周期问题已通过认证状态监听修复，并以 widget 测试覆盖离线/加载到在线的恢复。

本次证据只覆盖网络、认证和首页任务自动加载；采集、上传续传、图库、视频、报表、主数据同步仍需用户手动操作。正式服务器、域名和账号尚未确定，因此 v16 仍是局域网功能候选，`productionPublished=false`；模型精度、自动视频计数、三图自动去重和金蝶同步继续按原计划后置。

依据根目录 `盘点功能需求-新20250319(1).txt` 的八条需求。用户已确认 v12 续传成功，要求模型精度研究后置，优先完成功能并准备正式版本；正式服务器、域名、猪场与账号尚未确定。现有研究候选强制人工复核，模型与三视图自动去重不作为本轮可伪造的能力。

| 顺序 | 原始需求 | 当前缺口与交付 | 必要证据 |
|---|---|---|---|
| 1 | 1 主数据 | 管理端维护当前猪场编码/名称、栋舍/栏舍新增编辑启停；增量版本、管理员权限、幂等和审计 | MySQL 集成、代理白名单、管理端实际写入读回、手机同步 |
| 2 | 4/5 按栏日期图库 | 服务端查询与手机筛选/原图翻阅/删错图；确认锁定与管理员带理由覆盖继续权威 | 组织隔离、锁定冲突、真实读取与删除审计 |
| 3 | 6 历史与综合 | 日期任务/日报、指定栏舍跨日均值与参与日期，修复入口不足 | confirmed-only、日期范围、手机与管理端读回 |
| 4 | 2/3/7 采集闭环 | 先拍后导入已有图片、明确栏舍、ROI、离线采集续传与状态反馈收口 | 自动回归及集中真机验收 |
| 5 | 2/3/8 视频与多视图 | 核对视频证据的采集/保存/翻阅范围并实现合同允许的人工流程；视频推理与跨图自动去重随团队模型后续验收 | 不返回伪计数、不简单求和、证据锁定 |
| 6 | 正式交付 | 签名候选、独立部署配置、安全/备份/恢复/版本检查与操作说明 | 明确已验证与待目标环境验收；无正式目标时不声称已上线 |

金蝶正式 API 文档与账号未提供，保留适配边界和人工维护，不编造同步成功。保持当前栈，不复制 legacy 或 GPL/AGPL 代码。对照研究见 [功能收口对照](../research/functional-release-benchmark.md)。按涉及模块运行 AGENTS 门禁；现有 P0、手机草稿、签名身份、TLS 和锁定证据保留。

实现进展：主数据写入、服务器图库及两端入口、历史查询、删除/确认互斥保护、视频人工证据流程均已实现，Spring 69、Admin 24、Flutter 39、Inference 38 项及各模块构建/静态门禁通过；隔离管理端运行与视频 API/对象存储/真实 Worker 回调/确认锁定流程通过。手机新界面集中人工验收仍待完成，v14 安装结果待设备重新连接核实。正式交付配置及目标环境步骤见 [候选部署说明](../deployment/functional-release-candidate.md)。视频上限沿用网关 30 MiB；手机同时限制 2 分钟，后台拒绝视频自动候选数量。
