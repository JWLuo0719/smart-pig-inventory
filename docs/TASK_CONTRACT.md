# 任务契约：app-yolo

本文件用于编写可复用任务提示词与确认验收；AGENTS.md 的项目边界始终生效。

## 当前依据

- `docs/product/acceptance-criteria.md`（存在）
- `CURRENT_HANDOFF.md`（存在）

## 完成标准

按本次变更涉及的 Spring、Admin、Flutter、Inference 或基础设施模块执行 AGENTS 的 Verification；业务结果保持组织隔离、幂等、人工确认和历史版本不可变。APK 构建、真机联通、真实猪只验收和模型批准分别记录，任何一项不能替代另一项。

## 可直接改写的提示词

在本项目完成【具体用户行为/交付物】，范围为【模块或资料】。以 AGENTS.md 和上列对应事实源为准，仅加载当前动作所需技能。交付【文件/改动/报告】，按本任务对应的完成标准验证【成功场景、失败边界、所需环境】。报告实际证据与未验证项；推进到【本地完成/待人工审核/已授权外部阶段】后停止，不自动扩大为新项目、框架迁移、生产发布或真实用户数据操作。

## 交接与退役

新任务先核对当前工作树和状态文件。旧提示词中的版本、路径、阶段与测试数字是历史证据，不作为默认命令或新完成声明。已被现有实现替代的脚手架/重选技术栈提示停止使用；保留历史原文，不删除用户资料。只因文档治理发生修改，不应报告产品功能已重新通过验收。
