# 2026-09-12 Redmi K60 单图验收与上传清单修复

## 2026-09-13 非视频功能验收反馈

用户反馈前四项非视频功能测试成功，视频相关测试后置。该反馈作为人工验收记录保存；视频采集、上传、播放及自动计数不纳入本次通过结论，仍需后续独立验收。

## 2026-09-13 v18 步骤 2 自动恢复：通过

v18 (`0.1.0+18`) 同签名覆盖安装保留数据。原包 `775046d8-7ea4-4ade-91f8-878e90a7a94f` 未点击重试或放弃；应用启动后发现可同步队列并重新安排后台任务，服务端记录创建包 201、Blob 201、Manifest 201、Commit 201。数据库读回包 `e57c2e63-e39c-4660-b49b-da970f65de18` 为 `committed`，会话 `7b43bb4e-f062-4cf9-bee8-57dadf9a9e18` 为 `review_required`，候选 21、确认数为空。手机重启后的 UI 读回为“已提交服务器”、本机待上传 0、等待复核 1。

这证明本次“卡在创建上传包”已由应用自动恢复；候选 21 仍需用户用独立人工数复核后再确认，不作为模型精度或正式上线结论。证据：`artifacts/functional-v18-device-step2-final-ui.xml`、`artifacts/functional-v18-server-requests.log`、`artifacts/functional-v18-server-readback.txt`。

## 2026-09-13 步骤 2 上传包卡住：v17 修复

用户在步骤 2 保存一张新图后，上传队列出现“正在创建上传包”，未点击重试或放弃。该本地草稿包为 `775046d8-7ea4-4ade-91f8-878e90a7a94f`；最近网关日志没有对应的 `upload-packages` 请求，说明异常发生在移动端后台请求建立之前。应用日志显示 WorkManager 随后返回 `RETRY`，但旧实现没有清理 `creating_package` 租约，因而页面持续显示处理中。

v17 (`0.1.0+17`) 已修复：`SocketException`、TLS 握手异常和超时统一转为 `retry_wait`，清除租约并保留原图、包和幂等键；Drift 重试更新改用类型化时间绑定，避免 SQLite 绑定异常。新增回归测试覆盖网络套接字失败、自动退避、租约清理和安全错误文案；Flutter 全量测试 42 项、`flutter analyze` 和 Debug 构建通过。签名 LAN Release APK 已构建，SHA-256 为 `29569978fc4600be91534236ba2f282e546c56b7a0d10ec43d6187e919251fa0`。

本次修复不改变服务端已提交、已确认或已锁定的业务证据，也不要求清除应用数据。设备无线 ADB 在构建完成后暂时拒绝连接，v17 覆盖安装和该包状态的真机读回待设备重新连接；在重新连接前不要点击这条草稿的重试或放弃。

## 2026-09-13 步骤 1 手动首页/任务读回：通过

用户在 Redmi K60 上完成首页与任务页检查，截图显示网络连接已验证、今日真实任务 2 个（E2E-P01、E2E-P02），两项均为“未开始”。无线 ADB 实时 UI 读回再次确认存在“网络连接已验证”“今日真实任务”、P01/P02、本机待上传和等待复核；不存在“任务状态未知”和 `0 / 0`。网关在 05:04:08 完成 401→refresh 200→`/me` 200→任务 200，随后多次任务刷新均为 200；应用进程无 FATAL/ANR/Flutter 异常。设备当前无线调试端口为 `192.168.255.26:41177`，只作为临时连接信息，不写入 APK。

本步骤仅验证联网与任务投影，没有创建、确认、删除或锁定新的业务证据。UI 读回证据为 `artifacts/functional-v16-step1-ui.xml`。

## 2026-09-13 v16 冷启动、认证恢复与首页任务读回

- 设备：Redmi K60，`192.168.255.26:42187`；电脑局域网地址 `192.168.255.99`；APK `0.1.0+16`，SHA-256 `8d3b838b55974edd560e9c7dac33763bab17823edf782d030249122c7a8696b7`。覆盖安装保留数据，`firstInstallTime=2026-09-11 19:59:25`。
- 入口：`https://pig-inventory.local:8443`。mDNS/HTTPS 可达；网关记录 `/api/v1/me` 200、认证刷新后再次 `/me` 200、`/api/v1/inventory-tasks?businessDate=2026-09-13` 200。
- UI：`网络连接已验证`、`今日真实任务`、两个 E2E 任务、`0 / 2`、本机待上传 0、等待复核 0；没有“任务状态未知”或 `0 / 0`。完整 XML 见 `artifacts/functional-v16-device-ui.xml`。
- 发现与修复：原首页在认证状态仍为加载/离线时只请求一次，在线后不再刷新；`home_screen.dart` 现在监听认证状态转为在线并重新拉取任务。`home_task_state_test.dart` 三个场景均通过。
- 限制：系统拒绝 ADB `INJECT_EVENTS`，本轮未用脚本模拟点击。拍照/导入、续传、图库、视频、报表、主数据同步和现场实猪数据仍需用户手动集中验收；本记录不把首页读回当作完整真机验收。

## 已确认的故障

用户确认 v11 的“放弃失败草稿并重新采集”成功。新图在 13:27 创建采集包和上传 Blob 均返回 201，提交 manifest 返回 422；后续重试仍为 422。登录及主数据读取为 200。失败包保持 `awaiting_blobs`，已有一个上传完成的 Blob，没有盘点会话。

从受控存储读取该失败原图，在本机忽略目录用移动端原始哈希实现复现：哈希存在、格式校验失败、包含负号。合成均匀图回归也复现预期 `ffffffffffffffff` 实际为 `00000000000000-1`。原因是原生 Dart 的有符号 64 位整数溢出，`toUnsigned(64)` 后的字符串仍不能表达要求的无符号编码。既往递增亮度测试没有覆盖最高位为 1。

## 修复范围

- dHash 使用 BigInt 累积并生成固定 16 位小写十六进制；相似度算法、SHA-256、原图均不改变。
- 旧 Outbox 清单仅在发送时兼容历史负号编码，限定合法有符号 64 位范围。保持持久化原清单、采集包、资产、幂等键和原图；已上传 Blob 可直接续传清单与 Commit。合法哈希与其他非法值原样保留，由服务端继续校验。
- 本轮版本为 `0.1.0+12`，沿用签名身份和 LAN CA。模型仍是研究候选并强制人工复核。

## 自动验证

新增测试先复现失败，再验证修复；覆盖全 1 哈希、符号位边界、不误修合法/越界值，以及旧 blocked 草稿重试不新建包、不重传 Blob、不改变持久化清单和本机原图、保持幂等键且 Commit 成功。

`flutter analyze` 通过；全套测试 33 项通过，真实 TLS 项首次因未设置目录而跳过，随后设置 `LAN_TLS_DIRECTORY` 单独补跑两项均通过（共 34 个不同测试通过）；Debug APK 构建通过。失败原图在修复后的实现中格式合法且不含负号。原图及其哈希仅留忽略目录，不纳入本文。

运行中业务 JAR 与本地 JAR 的压缩包整体哈希不同，但逐项比较 `BOOT-INF/classes/` 应用内容无差异；Flyway V1–V11 均成功。P02 数据库仍为 confirmed、候选 19、确认 24。

## 签名与安装

v12 产物：`artifacts/android-release/20260912-133617-031/inventory-release-lan-acceptance.apk`，SHA-256 `59679743e4ab5943650b8cc69b92d2fd70a9a2193d9672ffc6aab3c5873e1055`。构建脚本验证同一发布证书、HTTPS 健康、非调试和禁用明文；无线无流式覆盖安装成功，设备回读 versionCode 12，首次安装时间仍为 2026-09-11 19:59:25。启动时 `/me` 401 后仅一次 refresh 200，再 `/me` 和任务接口 200，首页显示网络连接已验证。P02 确认 24 的会话仍保留，关联媒体 1 个且锁定。

ADB 模拟点击被手机系统以 `INJECT_EVENTS` 权限拒绝，没有调整安全设置。已请用户在上传队列对新图失败草稿手动点“重试”，等待服务端 manifest/Commit/候选及人工复核读回。候选计数与人工确认分别记录，不能把修复构建当作验收通过。

后续结果：用户确认续传成功；13:43:02 GET 原包 200，13:43:03 manifest 201、Commit 201，没有重复上传 Blob。数据库 committed / review_required，候选 20、未人工确认。用户要求精度优化等团队模型后置，本轮不继续样本精度研究。上述“等待重试”已解除。

Review Hub 检索零匹配，审计 `audit-20260912052816-14fa9f11` 已以真实续传及数据库证据完成。未声称经验改变结论。


## 功能候选 v14 交付与中断恢复

v14 同签名 Release 已构建，产物见 `artifacts/releases/20260912-functional-v14/`；39 项 Flutter 测试及静态、Debug/Release 门禁通过。此前 v13 真机冷启动显示 P02 人工确认 24，原安装身份与数据保留。v14 安装命令在中断前启动，结果未回收；18:56 恢复时无线 ADB 旧地址无法连接，设备安装与新界面操作保持待验收，不以构建替代。当前电脑已切换网络，旧 192.168.255.99/26 地址不再代表当前联通性。正式服务器、域名和账号未选定，未正式发布。
