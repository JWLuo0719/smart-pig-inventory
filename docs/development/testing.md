# 测试策略

测试围绕状态机和不可逆业务证据，而不是只追求覆盖率数字。

- 单元：哈希、ROI、平均算法、状态转换、权限决策、模型结果解析。
- 集成：MySQL 唯一约束、Flyway、MinIO、幂等请求、事务边界。
- 合同：OpenAPI 请求响应、推理 JSON Schema、Flutter DTO 兼容。
- E2E：断网采集、杀进程、续传、推理降级、复核、锁定、管理员覆盖。
- 回归：固定金标图片比较 PyTorch 与未来 ONNX/TFLite 的数量、框和置信度。

测试数据必须是合成或获授权数据，不能把真实猪场原图提交到仓库。

